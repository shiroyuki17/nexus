services:
  - type: web
    name: nexus-arena
    runtime: node
    plan: free
    region: singapore
    buildCommand: npm install && npx prisma generate && npm run build --prefix frontend && mv frontend/dist public
    startCommand: npx prisma db push && node backend/data/seed.js && node backend/server.js
    envVars:
      - key: DATABASE_URL
        value: postgresql://nexus:RiCn0ZrjgHtxLPpGphVaw10uyJjcWENh@dpg-d910g168bjmc739j9nc0-a/nexus_urth
      - key: NODE_ENV
        value: production
      - key: DEFAULT_COMPANY_ID
        value: demo-company
      - key: DEFAULT_COMPANY_NAME
        value: Demo Company
      - key: CORS_ORIGIN
        value: "*"
    healthCheckPath: /api/health
